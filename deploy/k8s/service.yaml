apiVersion: v1
kind: Service
metadata:
  name: trino-query-log-sink
  namespace: trino
  labels:
    app.kubernetes.io/name: trino-query-log-sink
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: trino-query-log-sink
  ports:
    - name: http
      port: 8080
      targetPort: http
